import {Box} from "@mui/material";
import React from "react";

const Protected = () => {
  return <Box sx={{
    my: 4,
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
  }}>Protected Panel Page</Box>;
};

export default Protected;